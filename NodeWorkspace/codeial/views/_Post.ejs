<li id="post-<%= post._id %>">

    <h3 class="text-primary d-inline mr-2"><%=post.content%></h3>

    <% if(locals.user && post.user.id == locals.user.id) { %>
        <a class=" delete-post-button btn btn-sm btn-danger " href="/posts/destroy/<%= post._id %>">Delete Post</a>
    <% } %>

    <form id="new-comment-form" action="/comment/create/<%= post._id %>" method="POST" class="form-inline mt-3">
        
        <div class="form-group mx-sm-3 mb-2">
            <label for="inputComment" class="sr-only">Comment</label>
            <input required type="text" class="form-control" id="inputComment" placeholder="comment" name="content">
        </div>
        <button type="submit" class="btn btn-primary mb-2">Submit</button>

    </form>

    <div id="comment-container-<%= post._id %>" class="ml-4 mt-3">
        <%   for(comment of post.comment) { %>
            <%- include('_comment') -%>
        <%   }; %>  
    </div>

    

</li>