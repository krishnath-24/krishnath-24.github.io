<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <title><%= title %></title>
</head>
<body>

    


  <nav class="navbar navbar-expand-lg bg-light p-3">
    <a class="navbar-brand" href="/">Habit-Rabit</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
  
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link" href="/view-weekly">Weekly</a>
        </li>
        
      </ul>
      
    </div>
  </nav>

      <form class=" container mt-5" action="/create-habit" method="POST">
        <div class="form-group">
          <h2 class="text-success">Add a Habit</label>

          <input type="text" name="description" class="form-control" id="habit-input" placeholder="eg : Go to gym" required>
        </div>
        
        <button type="submit" class="btn btn-primary">Submit</button>
      </form>

      <div class="container mt-5 d-flex flex-wrap">
        <% if(habits.length == 0) {%> 
            <p>You have no habits</p>
        <%} else {%>
        <% for(let habit of habits) {%>
            <div class="card mr-2 mb-3" style="width: 18rem;">
                <div class="card-body">
                    <h5 class="text-primary card-title"><%= habit.description %></h5> 
                    <p class="text-success card-subtitle">done : <%= habit.done %>/7</p>
                    <p class="text-success">Streak : <%= habit.streak %></p>
                <a class="btn btn-danger" href='/delete-habit/<%= habit._id %>' >Delete</a>

                </div>
            </div>
    
            <% } %>
        <%}%>
        
      </div>

      <script
  src="https://code.jquery.com/jquery-3.5.1.min.js"
  integrity="sha256-9/aliU8dGd2tb6OSsuzixeV4y/faTqgFtohetphbbj0="
  crossorigin="anonymous"></script>
      <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
    
</body>
</html>